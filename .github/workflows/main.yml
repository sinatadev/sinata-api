name: 🚀 Deploy API on push to college's vps
on:
  push:
    branches:
      - master
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "16"
      - name: Install Depedencies
        run: npm install

      - name: Deploy to VPS Server
        run: |
          ssh m3119016@203.6.149.153 'bash -s' << EOF
          cd /sinata-api
          git pull origin master
          npm install --production
          pm2 restart app.js
          EOF

      - name: Cleanup
        run: npm prune --production
